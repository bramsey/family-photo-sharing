<!DOCTYPE html>
<html>
  <head>
    <title>Make Grandpa Smile</title>
    <%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag :application %>
    <%= csrf_meta_tag %>
    <%= yield(:head) %>
  </head>
  <body>
    <div id="container">
      <%= render 'layouts/flashes' %>
	  <%= render 'layouts/session' %>
      <%= content_tag :h1, yield(:title) if show_title? %>
      <%= yield %>
    </div>
  </body>
</html>
